export const DifficultySettings = Object.freeze({
    easy: {
        fallSpeed: 0.0003,
        hitLeniency: 0.1,
    },
    medium: {
        fallSpeed: 0.0005,
        hitLeniency: 0.15,
    },
    hard: {
        fallSpeed: 0.001,
        hitLeniency: 0.1,
    },
    insane: {
        fallSpeed: 0.005,
        hitLeniency: 0.005,
    }
});