export const DifficultySettings = Object.freeze({
    easy: {
        fallSpeed: 0.002,
        hitLeniency: 0.5,
    },
    medium: {
        fallSpeed: 0.005,
        hitLeniency: 0.4,
    },
    hard: {
        fallSpeed: 0.010,
        hitLeniency: 0.3,
    },
    insane: {
        fallSpeed: 0.015,
        hitLeniency: 0.2,
    }
});